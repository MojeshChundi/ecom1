import React from "react";
import { Card } from "react-bootstrap";

function Cards(props) {
  return (
    <div>
      <Card bg="light">
        <Card.Title className="fs-1">The Generics</Card.Title>
      </Card>
    </div>
  );
}

export default Cards;
